# =============================================================================
# Core dependencies for scripts/ (production)
# Auto-generated by scanning active scripts/ imports (archives excluded).
# =============================================================================

# --- Document processing ---
python-docx>=1.1.0
python-pptx>=1.0.0
openpyxl>=3.1.0
lxml>=5.0.0
pypdf>=5.0.0
striprtf>=0.0.26
markitdown>=0.1.0

# --- Data & analysis ---
numpy>=1.26.0
scikit-learn>=1.4.0
beautifulsoup4>=4.12.0

# --- Image / OCR ---
Pillow>=10.0.0
pytesseract>=0.3.10
img2table>=1.3.0
pdfplumber>=0.10.0

# --- Platform-specific (Windows only, skipped on CI) ---
pywin32>=306; sys_platform == 'win32'

# --- Additional dependencies for newly added skills ---
scipy>=1.11.0
pandas>=2.0.0
statsmodels>=0.14.0
pingouin>=0.6.0
matplotlib>=3.8.0
lifelines>=0.30.0
pyDOE3>=1.0.0
requests>=2.31.0
